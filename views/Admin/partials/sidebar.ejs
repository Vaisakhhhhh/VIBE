<!-- Sidebar -->
<aside class="w-64 bg-black h-screen fixed">
  <div class="text-white text-2xl h-20 bg-black font-semibold mb-8">
    <img class="w-32 ml-12" src="../../images/vibe-logo.png" alt="Vibe Logo">
  </div>
  <nav class="p-4">
    <ul class="space-y-2" id="sidebar">
      <li><a href="/admin/dashboard" class="block p-2 text-white hover:bg-gray-900 delay-75 rounded" data-section="dashboard">Dashboard</a></li>
      <li><a href="/admin/products" id="product-sidebar" class="block p-2 text-white hover:bg-gray-900 delay-75 rounded" data-section="products">Products</a></li>
      <li><a href="/admin/users" class="block p-2 text-white hover:bg-gray-900 delay-75 rounded" data-section="customers">Customers</a></li>
      <li><a href="#" class="block p-2 text-white hover:bg-gray-900 delay-75 rounded" data-section="orders">Orders</a></li>
      <li><a href="#" class="block p-2 text-white hover:bg-gray-900 delay-75 rounded" data-section="coupons">Coupons</a></li>
      <li><a href="/admin/categories" class="block p-2 text-white hover:bg-gray-900 delay-75 rounded" data-section="category">Categories</a></li>
      <li><a href="/admin/brands" class="block p-2 text-white hover:bg-gray-900 delay-75 rounded" data-section="brands">Brands</a></li>
      <li><a href="#" class="block p-2 text-white hover:bg-gray-900 delay-75 rounded" data-section="banner">Banner</a></li>
      <li><a href="#" class="block p-2 text-white hover:bg-gray-900 delay-75 rounded" data-section="salesReport">Sales Report</a></li>
      <li><a href="#" id="logout-btn" class="block p-2 text-white hover:bg-gray-900 delay-75 rounded" data-section="logout">Logout</a></li>
    </ul>
  </nav>
</aside>

<!-- Logout Confirmation Modal -->
<div id="logout-modal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center hidden z-50">
  <div class="bg-white p-8 rounded-lg shadow-lg w-[400px]">
    <h2 class="text-xl font-semibold mb-4">Confirm Logout</h2>
    <p class="mb-6 text-gray-700">Are you sure you want to log out?</p>
    <div class="flex justify-end space-x-4">
      <button id="logout-cancel-btn" class="px-4 py-2 bg-gray-300 rounded-lg hover:bg-gray-400">Cancel</button>
      <button id="logout-confirm-btn" class="px-4 py-2 bg-black text-white rounded-lg hover:bg-gray-900">Confirm</button>
    </div>
  </div>
</div>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    const currentLocation = window.location.pathname;
    const menuItems = document.querySelectorAll("nav ul li a");

    // Highlight active menu item
    menuItems.forEach((item) => {
      if (item.getAttribute("href") === currentLocation) {
        item.classList.add("bg-[#22284f]");
      }
    });

    // Highlight product sidebar button if on product-related pages
    const productSidebarBtn = document.getElementById('product-sidebar');
    if (productSidebarBtn && (
      currentLocation.includes('/admin/products') ||
      currentLocation.includes('/admin/addproduct') ||
      currentLocation.match(/\/admin\/updateproduct\/\d+/)
    )) {
      productSidebarBtn.classList.add('bg-[#22284f]');
    }

    // Logout functionality
    const logoutBtn = document.getElementById('logout-btn');
    const logoutModal = document.getElementById('logout-modal');
    const cancelLogoutBtn = document.getElementById('logout-cancel-btn');
    const confirmLogoutBtn = document.getElementById('logout-confirm-btn');

    // Show logout modal
    logoutBtn.addEventListener('click', () => {
      logoutModal.classList.remove('hidden');
    });

    // Hide modal on cancel
    cancelLogoutBtn.addEventListener('click', () => {
      logoutModal.classList.add('hidden');
    });

    // Confirm logout
    confirmLogoutBtn.addEventListener('click', () => {
      window.location.href = '/admin/logout'; // Redirect to logout route
    });
  });
</script>
